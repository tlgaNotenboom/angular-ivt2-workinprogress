# Worked out example of an interface

## Overview

Example of a class that implements an interface in TypeScript. 

## Running

Compile and run `test_person.ts` to see that the class implements the interface.