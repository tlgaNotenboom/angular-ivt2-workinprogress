Wat we vandaag doen:

- Routing voorbeeld van laatste les kort bekijken
	>> Leg uit wat er gebeurt!
	>> CSS borders rond routeroutlet

- Users module maken
- Service toevoegen
- Push naar Heroku

- Observables behandelen


>> HUISWERK
- Bedenk vragen die op het tentamen gesteld zouden kunnen worden!
	Invullen via het online formulier.

- Routing naar New User/Edit existing user
	- Voeg een UserEdit component toe. Deze gebruik je voor zowel Edit als voor New!
	- Zorg dat je app op de juiste manier navigeert naar de UserEdit component
		- Onderscheid de status via data die je meegeeft aan de route.

		http://localhost:4200/users/0/edit én http://localhost:4200/users/list/3/edit
		- Edit user 0. 'Save' slaat wijzigingen op (console.log dit); 'Cancel' niet. 

		http://localhost:4200/users/new én http://localhost:4200/users/list/new
		- Nieuwe user; ook met Save en Cancel buttons.

		- Voeg de Edit en New routes toe op de User module

- Maak een UserService en simuleer (via console.log) CRUD functionaliteit op Users
	- Met meer entiteiten/modules komen er ook meer services bij.
	  Hoe kunnen we die services generiek maken?

- Deploy je service op Heroku, en vul je URL in in het online document.

- Bekijk de video 'YouTube - RxJs quick start with examples' - zie de link op Bb 
  bij Client-side web frameworks > Onderwijsmateriaal > Reactive streams.